import { createComponent } from "../utils";

const InputGroupLabel = createComponent({
  name: "InputGroupLabel",
  as: "label",
  classPrefix: "input-group-label",
});

export default InputGroupLabel;
